# templates/

Reusable starting points for new experiments.

To start a new experiment: copy `experiment_blueprint.md` into a new
folder under `../active_tests/<experiment-name>/` and fill it in as you
go.

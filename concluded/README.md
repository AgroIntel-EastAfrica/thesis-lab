# concluded/

Finished experiments, moved here from `../active_tests/` with their
`experiment_blueprint.md`'s **Results** section filled in.

A concluded experiment stays here as a record even if it "failed" —
a disproved hypothesis is still a real result worth keeping. Nothing here
is imported by or wired into the production app.
